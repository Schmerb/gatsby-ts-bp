/**
 * 
 * * {{ properCase name }} Messages
 *
 */

import { defineMessages } from 'react-intl';

export const scope = 'Components.{{ properCase name }}';

export default defineMessages({
  exampleMessage: {
    id: `${scope}.exampleMessage`,
    defaultMessage: '{{ properCase name }}',
  },
});
